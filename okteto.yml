name: okteto-test-build

deploy:
  image: ghcr.io/burstsms/pipeline-runner:main
  commands:
    - okteto deploy --dependencies --log-level debug --

dependencies:
  frontend:
    manifest: backend/test_domain/okteto.yml
    wait: true
    timeout: 15m